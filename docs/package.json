{
  "name": "docs",
  "version": "0.0.1",
  "packageManager": "pnpm",
  "scripts": {
    "start": "mkdocs serve",
    "build": "mkdocs build",
    "publish": "mkdocs gh-deploy",
    "install:pnpm": "pnpm install --fix-lockfile",
    "upgrade": "pnpm up -latest"
  }
}